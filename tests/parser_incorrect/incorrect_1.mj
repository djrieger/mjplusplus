class _ {

    /* No IDENT. */
    public _;

    /* No nested classes. */
    class _ {
    }

    /* Incorrect main method. */
    public static void _ (int[] _) {
    }
}

class _ {
    int i = 42;

    /* No ++ operator. */
    i++;

    /* No variable declaration in array access. */
    _[boolean foo = true];

    public void bar() {
        /* No Expression in second pair of brackets. */
        return new _[true = false][true = false][];
    }
}
