/**
 * This test is for if/while
 */

class Loops {

    public static void main ( String[] args)
    {
        (new Loops()).read();
    }

    public int read() {
        int a; int b; int c; int d; int x =3; int y = 3;

        a = -125;

        while (a < 10)
        {
            ;
            if ( a % 2 == 0 )
            {
                b = b+1;
            }
            else
            {
                c = c-1;
            }

            a = a + 3;
        }

        while ( (a = read()) != 0 )
        {
            d = a - a;
        }

        if ((d > 0) || (c < 0) || x < y)
        {
            return 1;
        }

        if (true) {} else if (false) {} else {} if (true) {{}};

        /* if - chain */
        if (true) if (true) if (false) if (true) if (false) if (true) ;

        /* while - chain */
        while(true) while(true) while(true) while(true) { while(true) while(true) while(true) while(true); }

        /* random blocks and empty statements*/
        /* the author is boooooooooored */

        {
            {
                ;{
                    ;
                };
            }
        }


        ;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;

        return 0;
    }
}
